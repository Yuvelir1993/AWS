{
  "name": "project-hub-backend",
  "version": "1.0.0",
  "description": "Backend for serving static files and API with Express and AWS S3 integration",
  "main": "dist/index.js",
  "scripts": {
    "start": "set S3_BUCKET_NAME=project-hub-bucket-green && pm2 start dist/index.js --name projectHubApp --env production",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.701.0",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "22.10.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.2"
  }
}